#ifndef ORCA_ANDROID_REAL_GCODE_PROCESSOR
#error "Android print G-code builds must compile with the real Orca GCodeProcessor."
#endif

namespace {
constexpr bool kAndroidRealGCodeProcessorGuard = true;
}
