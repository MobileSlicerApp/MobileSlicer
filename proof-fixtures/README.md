# Proof Fixtures

This directory is for manual or exploratory proof files.

Use checked-in files here for rerunnable local proof and benchmark steps after
the `_quarantine/root-dependency-dump*` cleanup. Do not point new docs or tests
at removed quarantine paths.

Do not add new automated expectations here. Promote repeatable fixtures to
`regression-fixtures/` with expected markers or a test harness reference.
